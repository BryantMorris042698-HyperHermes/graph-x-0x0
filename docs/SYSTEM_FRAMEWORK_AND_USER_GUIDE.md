# Graph_x_0x0 + AIOS Agent System
## Complete Framework & Simple User Guide

**For Bryant Issiah Morris Jr.**
**Truth Mode | Device-Aware | Cross-Platform (Phone / Tablet / Laptop)**

**Version:** 2026-06-13
**Repository:** https://github.com/BryantMorris042698-HyperHermes/graph-x-0x0

---

## 1. Executive Summary

This document describes the complete, unified AI Agent Operating System (AIOS) based framework built around **Graph_x_0x0** — your mathematical codebase analysis engine with regime optimization.

The system provides:

- A **Chat interface** for natural language conversation
- A **TUI Dashboard** for visual monitoring and control
- **Automatic device detection** and optimization for phone (Termux), tablet, and laptop
- Strict **identity protection** exclusive to Bryant Issiah Morris Jr.
- **Truth-only** operation grounded in deterministic mathematics (Φ(G)) and MemePlace retrieval
- Seamless integration with your existing tools: Hermes, MemePlace, Honcho, Holographic, and self-improving loops

The goal is a single, trustworthy, device-aware personal agentic layer that helps you govern and improve code architecture across all your devices.

---

## 2. System Architecture Overview

```
User (Bryant Issiah Morris Jr.)
          │
          ▼
   run.py (Unified Launcher)
          │
    Device Detection
          │
    ┌───────┴───────┐
    ▼               ▼
Phone (Termux)   Tablet / Laptop
Chat (primary)   Dashboard (primary)
    │               │
    └───────┬───────┘
            ▼
   GraphXTool (Core Engine)
            │
    ┌───────┴───────┐
    ▼               ▼
Deterministic     SLM (optional, grounded)
   Φ(G) / Regime    (via Hermes or llama.cpp)
            │
            ▼
   MemePlace (Truth Source)
            │
   AIOS Kernel (optional remote/local)
```

**Core Principle**: The mathematical engine (Graph_x_0x0) is always deterministic and truthful. The SLM is used only for natural language interface and is strictly constrained by Truth mode.

---

## 3. Core Components

### 3.1 Graph_x_0x0 Mathematical Engine
- Models any codebase as a directed graph G = (V, E)
- Computes live quality score: **Φ(G) = α·Q(G) − β·Č(G) − γ·mean(V)**
- Three optimization regimes (Simple, Advanced, Hybrid) with automatic switching
- Deviation detection using segmented regression
- Self-improving loop with measurable before/after Φ(G)

### 3.2 AIOS Integration
- AIOS (AI Agent Operating System) provides kernel services: scheduling, memory, tools, context
- Graph_x_0x0 is exposed as a first-class Tool inside AIOS
- Enables agent swarms (Honcho) and advanced memory (Holographic)

### 3.3 Interfaces
- **Chat Mode**: Natural language conversation (recommended on phone)
- **Dashboard Mode**: Visual TUI with gauges, theory, history (recommended on tablet/laptop)
- Both enforce identity and Truth mode

### 3.4 Device Auto-Detection
The launcher (`run.py`) automatically detects:
- Phone → Termux environment → lightweight Chat interface
- Tablet / Laptop → richer Dashboard interface
- Future: screen size, input method, and performance profiling

### 3.5 Identity & Truth Enforcement
Every session begins with verification that the user is **Bryant Issiah Morris Jr.**.
All outputs (whether from math or SLM) must be traceable to either:
- Direct computation from Graph_x_0x0, or
- Verifiable content from MemePlace

---

## 4. Simple Usage Workflows

### Workflow A: Daily Architecture Check (Phone)
1. Open Termux on Galaxy Z Fold7
2. Run the unified launcher in chat mode
3. Ask naturally: "How is the coupling in my current project?"
4. The system calls the engine, returns deterministic metrics, and provides a grounded explanation
5. Optionally request: "Run an improvement iteration"

### Workflow B: Deep Analysis Session (Laptop or Tablet)
1. On laptop or tablet, run the launcher
2. It automatically selects the full Dashboard
3. Use visual gauges to monitor Φ(G), Q, coupling, and cyclomatic complexity
4. Switch regimes manually or let the system recommend
5. Review Theory Mode and history of previous improvements

### Workflow C: Self-Improving Loop (Any Device)
1. Describe a change you made or want to make
2. The system measures baseline Φ(G)
3. Applies or simulates the change
4. Re-measures and decides whether to keep or rollback
5. Logs the outcome with explanation to MemePlace

### Workflow D: Using AIOS Kernel (Advanced)
- Run the kernel on your powerful machine (Omen)
- Run agents/chat on phone or tablet
- The launcher can connect to the remote kernel when `--aios` flag is used

---

## 5. Getting Started (High-Level)

### On Phone (Galaxy Z Fold7)
Follow the Termux bootstrap once. After that, simply use:
`python run.py --chat`

### On Tablet or Laptop
Create a virtual environment once, install dependencies, then run:
`python run.py`

The system will guide you through identity verification on first use of each session.

---

## 6. Integrations with Your Existing Ecosystem

- **Hermes** (Termux): Primary SLM backend on phone. The chat and dashboard can call Hermes for grounded responses.
- **MemePlace / MemPalace**: Source of truth for user-specific knowledge. All important decisions and explanations are logged here with spatial organization.
- **Honcho & Holographic**: Agent swarms and high-fidelity memory used when running under AIOS kernel.
- **Self-Improving Loops**: Built into both Chat and Dashboard. Every iteration is measurable via Φ(G).
- **Arch Linux / Hyprland (Omen)**: Full ratatui TUI and GPU acceleration available on laptop.

---

## 7. What Makes This System Different

- **Truth First**: No hallucinations. Everything is either mathematically computed or retrieved from your personal memory system.
- **Device Aware**: Automatically chooses the right interface and resource level for phone vs laptop.
- **Measurable**: Architecture quality (Φ(G)) is a first-class, optimizable metric — not just subjective opinion.
- **Personal**: Built exclusively for Bryant Issiah Morris Jr. with your projects, preferences, and memory as the foundation.
- **Unified**: One command (`python run.py`) works across all your devices.

---

## 8. Current Status & Roadmap

**Implemented and Ready**:
- Mathematical engine + regime system
- Chat and Dashboard interfaces
- Device auto-detection
- Identity + Truth mode enforcement
- AIOS Tool integration
- Cross-device state persistence
- Hermes / MemePlace hooks

**Next Priorities** (you can request any of these):
- Full ratatui TUI implementation for laptop
- Deeper real-time MemePlace synchronization
- Voice input on phone
- GPU-accelerated graph layout on Omen
- Background daemon mode with notifications

---

## 9. Support & Next Steps

This framework is designed to be simple to use while being technically deep.

If you want any part expanded (more detailed workflows, specific device setup, or new features), simply describe what you need and it will be added to the repository.

**Single command to start on any device after initial setup**:
`python run.py`

---

*Document prepared for Bryant Issiah Morris Jr. | HyperHermes ecosystem | 2026-06-13*